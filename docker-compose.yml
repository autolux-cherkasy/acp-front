services:
  frontend:
    build: .
    container_name: acp-frontend
    restart: unless-stopped
    env_file: .env
    environment:
      API_PROXY_TARGET: http://backend:3001
    networks:
      - acp-shared-network

networks:
  acp-shared-network:
    external: true
